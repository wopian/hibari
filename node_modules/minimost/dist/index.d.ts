/// <reference types="minimist" />
import { Opts } from 'minimist';
export default function (argv: string[], options?: Opts): {
    input: string[];
    flags: {
        [k: string]: any;
    };
};
